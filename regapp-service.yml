apiVersion: v1
kind: Service
metadata:
  name: tomcat-service
spec:
  selector:
    app: tomcat
  type: LoadBalancer   # or NodePort for internal
  ports:
    - protocol: TCP
      port: 90
      targetPort: 9090
